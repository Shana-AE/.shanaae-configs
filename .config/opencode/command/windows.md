---
description: Control the Windows desktop via Cua Driver (computer use) — e.g. "click that button in Notepad", "what's on screen right now", "automate this Windows app".
agent: windows-use
---

$ARGUMENTS
