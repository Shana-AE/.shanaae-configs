name: setup-configs
description: Generates actual configuration files from .example files by substituting placeholders with values from the .secrets file. Use this when you need to set up or refresh the configuration files (trae.json, opencode.json) with current secrets.
